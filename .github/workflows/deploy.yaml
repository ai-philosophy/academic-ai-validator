on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    permissions:
      contents: read
      packages: write
    env:
      API_BASE: ${{ secrets.API_BASE }}
      APP_CORS_ALLOWED_ORIGINS: ${{ secrets.APP_CORS_ALLOWED_ORIGINS }}
      GITHUB_MODELS_TOKEN: ${{ secrets._GITHUB_MODELS_TOKEN }}
      ACADEMIC_EVIDENCE_ENABLED: ${{ secrets.ACADEMIC_EVIDENCE_ENABLED }}
      ACADEMIC_EVIDENCE_MAX_CLAIMS: ${{ secrets.ACADEMIC_EVIDENCE_MAX_CLAIMS }}
      ACADEMIC_EVIDENCE_MAX_SOURCES_PER_CLAIM: ${{ secrets.ACADEMIC_EVIDENCE_MAX_SOURCES_PER_CLAIM }}
      
      CLOUDFLARE_TUNNEL_TOKEN: ${{ secrets.CLOUDFLARE_TUNNEL_TOKEN }}
    steps:
      - name: Checkout Develop
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor}}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Clean Container, Image And Prepare For Deploy
        run: docker compose -f docker-compose.prod.yml -p academic-ai-validator down --rmi all -v
      - name: Deploy Application
        run: docker compose -f docker-compose.prod.yml -p academic-ai-validator up -d --build
